# ─── Optional Overrides ──────────────────────────────────────
# Most config is auto-detected from vault-service/projects.json.
# Only set these if you need to override the defaults.

# Prism service URL (default: auto-detected from defaultHost)
# PRISM_URL=http://<YOUR_NAS_IP>:7777

# Node environment
# NODE_ENV=production
